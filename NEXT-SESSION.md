# На чём остановились — 12 августа 2026

> Записка для следующей сессии: контекст чистится, а хвосты остаются.

## Незакрытое (в порядке приоритета)

### 1. Девять запросов до входа

Открытая без сессии ссылка шлёт 9 запросов, каждый получает 401. Половину
(лишние повторы) уже убрал — было 18. Оставшиеся уходят по одному на ручку
**до того**, как приложение поймёт, что сессии нет.

Признак готов: `getSessionToken()` (см. `useDesktop.ts:142`). Часть хуков его
не проверяет — например, `NotificationBell.tsx` (0 проверок `enabled:`).
Правка: добавить `enabled: Boolean(getSessionToken())` там, где его нет.

---

## Сделано сегодня (в проде, проверено)

- **Короткая ссылка у ассистентов** — `shortCodeFor` звали из одного места:
  ручки шэра под сессией браузера, куда мост не дотягивается. Агент получал
  только длинный адрес (90 символов), а внутренний ассистент — вообще никакой.
  Теперь ответы про **одну** задачу несут `shortUrl`; списки — нет (первый
  вызов делает вставку в базу, и на выдаче из 50 задач это 50 записей впустую).
  Проверено на живой базе: 27 символов, повторный вызов даёт тот же код, код
  разбирается обратно в ту же задачу. Обновлены мост, внутренний ассистент,
  MCP, гайд и скил (запушен в `claude-skill-chatick`, версия в `mcp/` пересобрана).
- **Карточки уведомлений** — `line-clamp-2` стоял и не работал: ссылка на фигму
  это одно слово без пробелов, шире карточки, и переносить его браузеру негде.
  Карточка растягивалась на восемь строк и ломала ленту. Лечится `break-words`
  (сначала право разорвать слово, потом обрезка). Заголовку — две строки вместо
  `truncate`. То же исправлено на странице входящих.

- **Версии (фича)** — первый шаг: три таблицы (`project_features`, `releases`,
  `task_releases`), плюс `release_events` с обязательным комментарием к смене
  стадии. Стадии зашиты в код (`release-stages.ts`): iOS / Android / веб /
  бэкенд / десктоп / other. Домен прав `releases`, тумблер функции с ручками.
  Миграции 0071 и 0072 применены. **Следующий шаг: ручки CRUD и вкладка.**
  План — `PLAN-RELEASES.md`.
- **Упоминания** — механика работала везде, но ассистенты не знали формата и
  писали «@Таля» текстом, отчего уведомление не уходило никому. Формат
  `@[Name](userId)` теперь описан в `add_task_comment`, `create_task`,
  `chatick_task_create` и в общем каталоге гайда моста. Скил обновлён и
  запушен, локальная копия подтянута (нужен перезапуск Claude Code).
- **Обзор компании** — поиск проектов (от 8 штук), клик по столбику графика
  ведёт в проект, замок на проектах, где ты не в команде, и рядом имя того,
  у кого просить доступ (админы проекта — поля «создатель» у проекта нет, а
  роль owner не проставлена у 10 проектов из 11).
- **Честный текст вместо «проекта больше нет»** — 404 и 403 сводились в одно
  состояние, и админ компании, открывший чужой проект, читал «его удалили или
  у вас забрали доступ». Теперь 403 говорит «вы не в команде этого проекта».
- **403 от nginx** — оказался не от короткой ссылки. Во время выкатки `vite`
  чистит `dist`, и пока идёт сборка, `index.html` отсутствует: nginx пробует
  показать листинг папки и отдаёт 403. **Приложение лежало для всех** в это
  окно. Убрал `$uri/` из `try_files` и сделал выкатку атомарной —
  `/var/www/deploy-web.sh` на сервере (сборка в сторону, переключение
  переименованием). Проверено: 40 проб за время сборки, ни одной просадки.
  **Веб теперь выкатывать этим скриптом, а не `pnpm build`.**
- **18 запросов → 9** — `QueryClient` создавался без настроек, react-query
  повторял 401 трижды с задержками 1.3с → 3.4с → 7.5с.

---

## Хвосты за пользователем

- кракозябренный секрет в XFusion (`test` + сломанная подпись) — удалить;
- **ротировать** засвеченный JWT проекта и строку подключения к RDS;
- журнал drizzle отстаёт от файлов миграций (0068–0072 применялись вручную) —
  однажды привести в порядок;
- десктоп 0.3.9 не подписан; подпись планировалась через публикацию в магазин,
  плитки для appx уже сгенерированы в `apps/desktop/build/appx`.
