-- Чем собрана версия: профиль сборки (eas build --profile).
--
-- Отдельно от стадии намеренно. Стадия отвечает на «куда доехало» —
-- TestFlight, App Store, прод. Профиль отвечает на «чем собрали»: одна и та же
-- production-сборка проходит обе стадии, а preview-сборка может остаться на
-- первой навсегда. Свести их в одно поле значит потерять один из двух ответов.
--
-- Пусто — профиль не указан. Для веба и бэкенда он часто и не нужен.
ALTER TABLE "releases" ADD COLUMN IF NOT EXISTS "build_profile" text;
