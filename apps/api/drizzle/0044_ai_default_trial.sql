-- Умолчание источника ИИ — пробный, а не ключ компании.
--
-- У нового проекта ключа компании ещё нет, и выбирать его за человека значит
-- обещать то, чего нет: чат упирался в «ИИ не подключён», хотя пробный доступ
-- был готов. Существующие записи не трогаем — там осознанный выбор.

ALTER TABLE "project_ai" ALTER COLUMN "source" SET DEFAULT 'trial';
