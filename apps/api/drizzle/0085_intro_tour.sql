-- Отметка о вводном туре.
--
-- Nullable без умолчания: проставить всем «сейчас» значило бы объявить тур
-- пройденным для тех, кто его не видел, и они никогда бы его не получили.
-- Null читается как «не видел» — существующие пользователи увидят его при
-- следующем заходе в проект, а это ровно то, что нужно: интерфейс им тоже
-- никто не объяснял.
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "tour_seen_at" timestamptz;
