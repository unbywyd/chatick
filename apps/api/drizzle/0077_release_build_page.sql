-- Страница сборки в EAS — отдельно от ссылки на артефакт.
--
-- Отвечают на разное: referenceUrl это «скачать сборку», buildPageUrl —
-- «посмотреть логи и статус». Когда сборка упала, артефакта нет вовсе, а
-- страница с логами и есть то единственное, что нужно человеку.
ALTER TABLE "releases" ADD COLUMN IF NOT EXISTS "build_page_url" text;
