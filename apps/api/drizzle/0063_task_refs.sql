-- Свои номера задачи (SPEC §8.6).
--
-- То, чем задачу зовут ВНЕ Chatick: экраны в макете, пункты договора, позиции
-- сметы. Строка, а не число и не связь с другой таблицей: у каждой команды
-- свой счёт, и «12 - 14» у одних значит диапазон, у других — составной номер.
-- Разбирать это на сущности значило бы навязать им одну трактовку.
--
-- Пусто по умолчанию: у большинства задач внешнего номера нет и не будет.
ALTER TABLE "tasks" ADD COLUMN IF NOT EXISTS "refs" text DEFAULT '' NOT NULL;
