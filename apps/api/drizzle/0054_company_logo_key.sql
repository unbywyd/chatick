-- Логотип компании как файл, а не ссылка (SPEC §8.40).
--
-- logo_url у компании был, но заполнить его можно было только руками через
-- API — загрузки не существовало. При этом логотип теперь показывается в
-- шапке вместо нашего, и просить человека «где-то разместить картинку и
-- прислать ссылку» — не решение.
--
-- Ключ объекта в хранилище, как у проектов: файл приватный, раздаётся через
-- свою ручку, а версия в адресе не даёт кэшу отдать старую картинку.
ALTER TABLE "companies" ADD COLUMN IF NOT EXISTS "logo_key" text;
