-- Разрешение отправлять модели картинки.
--
-- Выключено по умолчанию: не всякая модель умеет смотреть изображения, а та,
-- что не умеет, отвечает ошибкой на весь запрос — человек получает «не
-- получилось» вместо ответа и не понимает, почему. Пусть включает осознанно.
--
-- У компании, рядом с моделью: вижен — свойство модели, а она одна на все
-- проекты компании.
ALTER TABLE "companies" ADD COLUMN IF NOT EXISTS "llm_vision" boolean DEFAULT false NOT NULL;
